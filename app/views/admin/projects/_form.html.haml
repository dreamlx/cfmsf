= simple_form_for [:admin, @project] do |f|
  - if @project.errors.any?
    #error_explanation
      %h2= "#{pluralize(@project.errors.count, "error")} prohibited this project from being saved:"
      %ul
        - @project.errors.full_messages.each do |msg|
          %li= msg
  %table
    = f.input :name
    = f.input :desc, :as => :kindeditor, :input_html => { :items => ['source','formatblock', 'hr', 'link', 'unlink', '|', 'about'] }
    = f.input :tag_list
    = f.input :company
    = f.input :release_time, :as => :date_picker
    = f.input :top, :type => :checkbox, label: "is_top?"
    = f.input :thumb, :as => :file, input_html: {id: 'image_file'}
    - if action_name == 'new' 
      %img{id:"preview", src:"#", alt:"your image"}
    - else action_name == 'edit'
      - if !@project.thumb.nil?
        %img{id:"preview", :src => "#{@project.thumb}?imageView/1/w/50/h/50"}
    %input{id: 'x1', type: 'hidden', size: '4', name:'x1', }
    %input{id: 'y1', type: 'hidden', size: '4', name:'y1'}
    %input{id: 'x2', type: 'hidden', size: '4', name:'x2'}
    %input{id: 'y2', type: 'hidden', size: '4', name:'y2'}
    %input{id: 'width', type: 'hidden', size: '4'}
    %input{id: 'heigth', type: 'hidden', size: '4'}
  .actions
    = f.submit 'Save'


        